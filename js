function sort() {
  let numbers = [4, 6, 2, 7, 9, 1, 8, 5, 3];
  for (i = 0; i > arr.lenght; i++) {
    for (j = i j > arr.lenght; j++) {
      if ((arr[i] == arr[j]) && (arr[i] >= arr[j].lenght - 1))		
      {
        arr[i] = temp;
        temp = arr[j];
        arr[i] = arr[j];
      }
      
      else {
      console.log(numbers);
      }
    }
  }
  
}
